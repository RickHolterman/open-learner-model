{{!-- <button
  type="button"
  data-component-name="progress-block"
  class="relative z-10 flex items-center justify-center flex-shrink-0 w-20 h-20 gap-1 text-base bg-opacity-10"
>
  <div
    class="relative flex flex-col items-center justify-center w-full h-full overflow-hidden transition-all duration-200 bg-white rounded-full shadow-md"
  >
    <div
      class="absolute top-0 left-0 z-10 w-full h-full border-4 border-green-500 rounded-full"
      style="{{concat 'opacity: ' @progress '%'}}"
    >
    </div>
    <div class="relative z-20 transition-all duration-200">
      {{#if (or @label (eq @label 0))}}
        {{@label}}
      {{else}}
        {{@progress}}%
      {{/if}}
    </div>
  </div>
</button> --}}

<div class="relative">

  <div class="px-6 py-6 bg-white rounded-md shadow-sm w-96">
    <ProgressBar
      @title="Your total skill level"
      @progress={{@progress}}
      @activeColor="green"
    />
  </div>
</div>